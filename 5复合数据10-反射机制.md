### 一：反射机制概述
1. go语言提供了一种机制，能够在运行时更新变量或检查它们的值、调用它们的方法和支持的内在操作，不需要再编译时就知道这些变量的具体类型。
2. 这种机制被称为反射。
3. 反射可以让我们讲类型本身作为第一类的值类型处理

### 二：reflect.ValueOf(i interface{})
1. 用来获取输入参数接口中的数据的值，如果接口为空则返回0

### 三：reflect.TypeOf(i interface{})
1. 用来获取输入参数接口中的值的类型，如果接口为空则返回nil
